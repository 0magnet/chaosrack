//go:build js && wasm

package attractor

import (
	_ "embed" // for the //go:embed directives below
)

// ── Controls panel HTML ──────────────────────────────────────────────────────

//go:embed panel.css
var panelCSS string

// controlsBody is the panel markup; the stylesheet lives in panel.css
// (embedded above) and is prepended as a <style> block at inject time.
const controlsBody = `
<div class="modules">
<div class="sect console"><div class="sect-hdr" title="Console — model selection, global actions, and every mode/effect switch in one module">Console</div>
<div class="row toprow swrow swsecs">
  <div class="swsec consec"><div class="swsec-hdr" title="Equation editing and global panel actions. The model itself is chosen on its own category's row — see the rotary at the head of each model bay — rather than from a knob up here that changed what the whole instrument was.">Model</div>
  <!-- The single source of truth for which model is running. It has no face
       of its own any more: the two concentric knobs that used to drive it
       sat on the Console, so the one control the whole rack is about was
       nowhere near the controls it governed, and the rack had two model
       selectors to keep in step with each other. The category rows are the
       face now; this is the wire they all drive. -->
  <select id="mode-select" style="display:none"></select>
  <label class="grp" style="cursor:pointer;" title="Load the current attractor's equations into the editable Custom mode; toggle off to return to it. It is a model choice — it moves the model knob to Custom — so it sits with the knob it moves."><input type="checkbox" class="sw" id="edit-eq-sw"> Edit eqn</label>
  <span id="extra-nav"></span>
  <div class="console-btns">
  <span class="grp btn-row"><button class="pushbtn" id="reset-all-btn" title="Reset parameters, colors, view pose, trail, gradient, effect switches (Front/Fill/audio/backdrops), display style, and the signal generator to their defaults (keeps your dock layout + interface size)"></button><span class="btn-lbl">Reset All</span></span>
  <span class="grp btn-row"><button class="pushbtn" id="normalize-btn" title="Re-center the model to the default (identity) orientation and stop any spin"></button><span class="btn-lbl">Normalize</span></span>
  </div>
  </div>
  <div class="swsec"><div class="swsec-hdr" title="Audio — the two subsystems that are switched on rather than patched in: the modulation bus, and hardware control over it.">Audio</div>
    <label class="grp" style="cursor:pointer;" title="Enable modulation — reveals the MOD + EQ modules, where each control can be routed from a source with a signed depth. The sources are the audio features (a channel and a band curve) and the MODEL ITSELF: the attractor's own x, y, z or radius driving its own constants, which closes the loop and makes a different system out of the one named on the dial."><input type="checkbox" class="sw" id="audio-mod"> Mod</label>
    <label class="grp" style="cursor:pointer;" title="WebMIDI — hardware control: CC 1..N drive the current mode's parameter knobs in order, CC 21..28 the view targets (zoom, pans, spins, rainbow, trail), and any note hops to that note's attractor."><input type="checkbox" class="sw" id="midi-sw"> MIDI</label>
  </div>
  <div class="swsec"><div class="swsec-hdr" title="Rows — which model categories are in the rack. A row carries its own monitor and the constants of every model in the category, which is 213 controls across the eleven of them and most of what the panel costs to draw. A row that is out costs nothing at all: no layout, no paint, no measurement. The models in it still play — this puts the front panel away, not the instrument.">Rows</div>
    <span id="row-switches" class="rowsw"></span>
  </div>
  <div class="swsec"><div class="swsec-hdr" title="Rack — the frame itself and the window it is drawn in, rather than anything in the signal path. Nothing here has a knob elsewhere to be beside, which is why it is the one group of switches that stays central.">Rack</div>
    <label class="grp" style="cursor:pointer;" title="Power — the render loop. Off stops it and clears the canvas, which is what the GPU costs; the panel stays, so every setting is still there to read and to change. A switch again, and back in the Rack group where the rest of the frame's own controls are: it had been folded into the model knob's first detent, which made powering down look like choosing a model called OFF and put the rack's power switch inside the one control that had nothing to do with the rack."><input type="checkbox" class="sw" id="power-sw" checked> Power</label>
    <label class="grp" style="cursor:pointer;" title="Rack bay — draw the 19-inch frame each ROW of modules sits in: rails above and below, an ear each side, and blank panels filling the leftover at the end of the row on the same slot pitch. The modules wrap, and a row is a bay, so a rack that has spilled onto a second row gets a second frame. Handles and panel screws are chosen on the Style module: the inner ring of Size."><input type="checkbox" class="sw" id="handles-on"> Rack bay</label>
    <label class="grp" style="cursor:pointer;" title="Scope — bolt the rack-mount oscilloscope into the frame. It is a UNIT, not a plug-in module: a 19-inch instrument with its own tube, timebase and trigger, fed from the live audio and running whatever the MODEL knob is pointed at. This switch is the only thing that puts it in or takes it out."><input type="checkbox" class="sw" id="scope-on"> Scope</label>
    <label class="grp" style="cursor:pointer;" title="Show the Template module — a labeled legend of every named slot a module can have (header, label, readout, knob, ring, inner, fine, dial, reset). Hover a slot for the Go struct field it maps to. It is documentation about what a module can hold rather than an instrument, which is why it is the one module still on a switch."><input type="checkbox" class="sw" id="tpl-on"> Template</label>
    <label class="grp" style="cursor:pointer;" title="Overlay a short description of the current attractor / model on the view"><input type="checkbox" class="sw" id="show-info"> Info</label>
    <label class="grp" style="cursor:pointer;" title="Toggle browser fullscreen — the canvas fills the display; the panel stays available"><input type="checkbox" class="sw" id="fullscreen-sw"> Fullscreen</label>
    <label class="grp" style="cursor:pointer;" title="Desk — run this app inside a desktop: the desk becomes the environment, its panel takes the bottom of the screen, and the rack floats as a window on it with a task button beside the desk's own. The scene keeps running behind. Open a terminal, the host shell or the file manager from the desk's Applications menu. The OTHER way to have the desk is as a MODEL (Solids &#8594; desk), where the whole desktop is drawn into the scene and turns with it."><input type="checkbox" class="sw" id="desk-contain"> Desk</label>
  </div>
</div>
</div>
<!-- The scope is a rack UNIT, not a module. It is a 19-inch instrument
     bolted to the rails, not a card in a subrack opening, and wrapping
     it in a .sect made it the second of those — which is why it needed
     a full-row span override to look right. It sits here in source
     order and relayoutInstrumentUnits bolts it into a unit of its own.
     A later revision that takes plug-ins gets an opening beside this
     panel and needs nothing new from the container model. -->
<div class="scope-panel" id="scope-panel">
    <div class="scope-tube"><canvas id="scope-screen" width="480" height="384"></canvas><div class="scope-glass"></div></div>
    <div class="scope-ctl">
      <div class="scope-brand"><b>CHAOSRACK</b><span>TYPE 1 · DUAL TRACE</span></div>
      <div class="scope-grp" title="Vertical — how much signal one division of height is worth, and where the trace sits on the screen. The range switch is detented 1-2-5; a trace that runs off the top means the range is too fine, which is the fault the knob exists to find."><div class="scope-grp-hdr">VERTICAL</div>
        <span class="scope-knob"><span class="scope-lbl">VOLTS/DIV</span><span id="scope-volts-stack"></span><select id="scope-volts" style="display:none"></select><span class="scope-read" id="scope-volts-read"></span></span>
        <span class="scope-knob"><span class="scope-lbl">POSITION</span><span id="scope-vpos-stack"></span><input type="range" id="scope-vpos" min="-4" max="4" step="0.05" value="0"></span>
        <span class="scope-knob"><span class="scope-lbl">SOURCE</span><span id="scope-chan-stack"></span><select id="scope-chan" style="display:none"></select><span class="scope-read" id="scope-chan-read"></span></span>
      </div>
      <div class="scope-grp" title="Horizontal — the timebase. TIME/DIV is how long the beam takes to cross one division, so ten of them is the whole screen; POSITION slides the swept window earlier or later within what was captured."><div class="scope-grp-hdr">HORIZONTAL</div>
        <span class="scope-knob"><span class="scope-lbl">TIME/DIV</span><span id="scope-time-stack"></span><select id="scope-time" style="display:none"></select><span class="scope-read" id="scope-time-read"></span></span>
        <span class="scope-knob"><span class="scope-lbl">POSITION</span><span id="scope-hpos-stack"></span><input type="range" id="scope-hpos" min="-5" max="5" step="0.05" value="0"></span>
      </div>
      <div class="scope-grp" title="Trigger — what makes a repeating waveform stand still. The sweep starts at the first crossing of LEVEL in the SLOPE direction. AUTO sweeps anyway when nothing crosses, so silence shows a baseline instead of a blank tube; NORM waits, which is what you want when you are looking for a rare event."><div class="scope-grp-hdr">TRIGGER</div>
        <span class="scope-knob"><span class="scope-lbl">LEVEL</span><span id="scope-trig-stack"></span><input type="range" id="scope-trig" min="-1" max="1" step="0.01" value="0"></span>
        <span class="scope-knob"><span class="scope-lbl">MODE</span><span id="scope-tmode-stack"></span><select id="scope-tmode" style="display:none"></select><span class="scope-read" id="scope-tmode-read"></span></span>
        <label class="scope-sw" title="Slope — which way the signal must be going through the level for the sweep to start."><input type="checkbox" class="sw" id="scope-slope" checked><span>SLOPE &#8599;</span></label>
      </div>
      <div class="scope-grp" title="Display — the tube itself. INTENSITY is beam brightness and how long the phosphor holds; FOCUS is how tight the spot is."><div class="scope-grp-hdr">DISPLAY</div>
        <span class="scope-knob"><span class="scope-lbl">INTENSITY</span><span id="scope-intens-stack"></span><input type="range" id="scope-intens" min="0.05" max="1" step="0.01" value="0.6"></span>
        <span class="scope-knob"><span class="scope-lbl">FOCUS</span><span id="scope-focus-stack"></span><input type="range" id="scope-focus" min="0" max="1" step="0.01" value="0.55"></span>
        <label class="scope-sw" title="Beam on. Off blanks the tube and stops the sweep — the scope stays in the rack with its screen dark, which is what a scope that is switched off looks like."><input type="checkbox" class="sw" id="scope-beam" checked><span>BEAM</span></label>
      </div>
    </div>
  </div>
<div class="sect" id="params-module"><div class="sect-hdr" title="Parameters — the current model's tunable constants (each with knob, LED value, step size, and reset)">Parameters</div><div id="params" class="row"></div></div>
<div class="sect" id="layers-module"><div class="sect-hdr" title="Layers — where a second picture goes relative to the model. BEHIND draws one filling the canvas behind it; SKIN paints the spectrogram onto the model's own surface; FILL makes the spectrogram or FVF plane the whole screen, face-on, instead of a plane you can turn. Skin and Fill dim when the current model cannot take them.">Layers</div>
<div class="row vmrow">
    <span class="pcell axcol rstcell" id="bg-cell" title="Backdrop — what is drawn behind the model, filling the canvas: the scrolling spectrogram, the XY oscilloscope, a live terminal, or the whole desk. Water is the exception: it is a LENS, drawn through the finished frame rather than behind it — drag on the canvas to make waves. One at a time, or off."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">Behind</span><span id="bg-stack"></span></span></span><button class="rst" id="rst-bg-visual" title="Reset the backdrop to off">↺</button><select id="bg-visual" title="Backdrop — what is drawn behind the model" style="display:none"><option value="" title="off — nothing behind the model; the background color alone">off</option><option value="spectrogram" title="spectrogram — the scrolling spectrogram of the live audio fills the canvas behind the model">spectrogram</option><option value="xy" title="xy scope — the stereo XY oscilloscope (a goniometer) behind the model">xy scope</option><option value="terminal" title="terminal — a live terminal, running a real shell, behind the model">terminal</option><option value="termanim" title="animation — a terminal animation plays behind the model">animation</option><option value="desk" title="desk — the whole 3-D desktop, windows and all, behind the model">desk</option><option value="water" title="water (lens) — the exception: water is drawn THROUGH the finished frame rather than behind it; drag on the canvas to make waves">water (lens)</option></select></span>
    <span class="pcell axcol rstcell" id="skin-cell" title="Skin — what is painted ON the model's own surface, wrapped round it, for the models that have a surface to take it: the live spectrogram, a terminal, or the whole desk. Off leaves the wireframe. The same three pictures the Behind knob draws in the background; this puts one on the object instead."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">On</span><span id="skin-stack"></span></span></span><button class="rst" id="rst-skin-visual" title="Reset the skin to off">↺</button><select id="skin-visual" title="Skin — what is painted on the model's surface" style="display:none"><option value="" title="off — the model keeps its wireframe; nothing is painted on it">off</option><option value="spectrogram" title="spectrogram — the live spectrogram is wrapped round the model's surface">spectrogram</option><option value="terminal" title="terminal — a live terminal is wrapped round the model's surface">terminal</option><option value="desk" title="desk — the whole 3-D desktop is wrapped round the model's surface">desk</option></select></span>
  <span class="pcell axcol" id="layers-sw">
    <label class="grp" style="cursor:pointer;" title="Fill the screen with the spectrogram / FVF display (face-on) instead of the rotatable plane"><input type="checkbox" class="sw" id="spect-fill"> Fill</label>
  </span>
</div></div>
<div class="sect" id="desk-module" style="display:none"><div class="sect-hdr" title="Desk — the desktop's settings, wherever it is on screen: as the model, as the backdrop behind another model, or as the environment this app runs inside. It appears when any of those is true and goes away with them. MOVING WINDOWS while the desk is the MODEL: ctrl-drag reaches the desk, so ctrl-drag a title bar to move a window and an ordinary drag still turns the model. Pass-thru swaps the two, if you would rather drag windows directly and hold ctrl to turn.">Desk</div>
<div class="row vmrow">
  <span class="pcell axcol rstcell" id="desk-style-cell" title="Desktop style — four 3-D desktops reproduced over the scene. Flat: ordinary windows. Looking Glass (Sun, 2003): windows lean back in a legible stack; double-click a title bar to turn one over and read its back. Cube (Compiz, 2006): four workspaces on the faces of a cube; the arrow keys spin it. Metisse (2004): shift-drag a title bar to turn a window freely, and it stays live while turned. BumpTop (2009): windows have weight and fall into a pile on top of the rack."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">style</span><span id="desk-style-stack"></span></span></span><button class="rst" id="rst-desk-style" title="Reset the desktop style">↺</button><select id="desk-style" class="selwin" title="Desktop style — how the windows are drawn" style="display:none"></select></span>
  <span class="pcell axcol">
    <label class="grp" style="cursor:pointer;" title="Pass-through — where the mouse goes while the Desk MODEL is on screen. Off: dragging turns the model, and Ctrl-drag reaches the desk. On: dragging reaches the desk — moving windows, resizing them, pressing their buttons — and Ctrl-drag turns the model. Control always inverts the switch, so both gestures are available either way. The keyboard needs neither: double-click to type into the focused window, Esc to give it back."><input type="checkbox" class="sw" id="desk-pass"> Pass-thru</label>
  </span>
<div class="row"><span class="pcell axcol" id="desk-monitor-cell" title="Desk monitor — the desktop as it is, face on, whatever the model is doing. The desk drawn as a model turns with the model, which is the point of it and also means a window you are working in can be edge-on or behind you; this always shows it flat. Same idea as the Record module's monitor."><span class="punit-top"><span class="plabel">monitor</span></span><canvas id="desk-monitor" width="244" height="150"></canvas></span></div>
  <label class="grp" style="cursor:pointer;" title="Monitor — power to this module's screen, for the reason the Record monitor has one: it is a canvas copy every frame, and a screen nobody is watching should not cost the model anything."><input type="checkbox" class="sw" id="desk-mon-on" checked> Monitor</label>
</div></div>
<div class="sect" id="spectro-module" style="display:none"><div class="sect-hdr" title="Spectro — the spectrogram's own controls, for when it is a LAYER rather than the model: painted on a surface as the skin, or filling the canvas behind the model as the backdrop. As the model its controls are in Parameters, like every other model's; in either of the other two roles Parameters is showing something else, and these had nowhere to be at all.">Spectro</div>
<div id="spectro-params" class="row"></div></div>
<div class="sect" id="pong-module" style="display:none"><div class="sect-hdr" title="Scoreboard — Scope Pong's front panel: each player's score over their paddle pot (turn it to seize the paddle from the machine; it spins by itself while the machine plays, like a motorized pot), plus a restart button">Scoreboard</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">left</span><span class="led demo-led" id="pong-score-l" title="Left player's score (W/S, left-half touch, or the pot below) — first past 9 resets the match">0</span></span><input type="range" id="pong-pad-l" min="-1" max="1" step="0.01" value="0" title="Left paddle pot — turn to take the left paddle from the machine; tracks the paddle while the machine or keys drive it" style="display:none"><span class="grp vmbay"><span id="pong-lstack"></span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">right</span><span class="led demo-led" id="pong-score-r" title="Right player's score (↑/↓, right-half touch, or the pot below) — first past 9 resets the match">0</span></span><input type="range" id="pong-pad-r" min="-1" max="1" step="0.01" value="0" title="Right paddle pot — turn to take the right paddle from the machine; tracks the paddle while the machine or keys drive it" style="display:none"><span class="grp vmbay"><span id="pong-rstack"></span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="grp btn-row demo-btn"><button class="pushbtn" id="pong-restart" title="Restart the match — zero both scores and serve fresh"></button><span class="btn-lbl">Restart</span></span></span>
</div></div>
<div class="sect" id="stext-module" style="display:none"><div class="sect-hdr" title="Banner — Fourier Text's input: what the harmonic character generator writes (A–Z, 0–9, dash, space). The harm knob in Parameters sets how many harmonics each glyph keeps.">Banner</div>
<div class="row keysflex stext-row">
  <span class="pcell axcol vmcell gen-cell stext-cell"><span class="punit-top"><span class="plabel">text</span></span><input type="text" id="stext-in" class="stext-in" maxlength="24" data-no-drag title="Banner text — what the beam writes; melt it with the harm knob in Parameters"></span>
</div></div>
<div class="sect" id="smorph-module" style="display:none"><div class="sect-hdr" title="Patch — the self-programming analog computer's wiring readout: which two catalog systems the machine is blended between right now">Patch</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">wired</span></span><span class="grp vmbay"><span class="led demo-led smorph-led" id="smorph-led" title="Live patch — the current catalog blend, e.g. D-E 42% (100% = fully the next system); the sys knob parks it, the rate knob self-steps">D</span></span></span>
</div></div>
<div class="sect" id="bounce-module" style="display:none"><div class="sect-hdr" title="Launcher — Bouncing Ball's front panel: the drop-height pot (the analog demo's initial-condition setting), the machine's re-kick count, and a Drop button">Launcher</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">height</span><input type="number" class="numin gen-led" id="bounce-height-led" title="Drop height (0.2–1.0, court units above the floor) — where the next Drop releases the ball" min="0.2" max="1" step="0.05" value="0.90"></span><input type="range" id="bounce-height" min="0.2" max="1" step="0.05" value="0.9" title="Drop height pot — the initial condition the next Drop (or mode entry) releases the ball from" style="display:none"><span class="grp vmbay"><span id="bounce-hstack"></span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">kicks</span><span class="led demo-led" id="bounce-kicks" title="Machine re-kicks since the mode started — each is a decayed ball re-launched">0</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="grp btn-row demo-btn"><button class="pushbtn" id="bounce-drop" title="Drop the ball again from the height pot's setting with a fresh drift"></button><span class="btn-lbl">Drop</span></span></span>
</div></div>
<div class="sect" id="stlfile-module" style="display:none"><div class="sect-hdr" title="Loader — STL File's front panel: load a stereolithograph (.stl, binary or ASCII) from disk and it renders as a rotating wireframe through the normal pipeline (gradient, spin, camera fit and Model Out all apply)">Loader</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">file</span><span class="led demo-led stl-led" id="stlfile-led" title="Loaded STL — file name and triangle count (a stand-in cube shows until a file is loaded)">none</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="grp btn-row demo-btn"><button class="pushbtn" id="stlfile-load" title="Load an .stl file from disk (binary or ASCII)"></button><span class="btn-lbl">Load</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">built-in</span></span><span class="grp vmbay"><select id="stlfile-builtin" class="stl-builtin" title="Built-in solids — the rack modules and the 19-inch frame at their real dimensions, the geometry as closed solids, and every attractor swept as a tube along its own trajectory. Generated in the browser; the same models are written as .stl files by cmd/stlgen."><option value="">— load a file —</option></select></span></span>
</div></div>
<div class="sect" id="termanim-module" style="display:none"><div class="sect-hdr" title="Animation — Terminal Animation's front panel: which of tuiwasm's terminal programs is drawn on the quad">Animation</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">animation</span></span><span class="grp vmbay"><select id="termanim-pick" class="stl-builtin" title="Which terminal program runs on the quad. Twenty-one animations plus the text demos, from tuiwasm; the list is built from what is registered, so it cannot drift from what is actually there."></select></span></span>
</div></div>
<div class="sect"><div class="sect-hdr" title="Colors — SRC (what the color follows, including OFF for a flat trace), MAP (how a value becomes a color, for every display including the spectrogram), the map window (period and shift), and trail length">Colors</div>
<div class="row vmrow">
  <span class="pcell axcol rstcell" id="src-cell" title="Source — what the color follows. OFF is a flat trace in the start swatch: the color follows nothing, which is what used to be called the mono palette and is a statement about the SOURCE rather than about the map. X / Y / Z follow a coordinate of the figure; on the three delay embeddings those axes are one signal at three lags, so they color along different directions of the same figure rather than showing three different quantities. TRAIL follows age along the window. AUDIO paints the short-time spectral CENTROID along the trail — which frequency each moment sits at. LEVEL paints the short-time LEVEL instead, which is the quantity the spectrogram backdrop is painting, so with one of the colormap palettes behind it the figure and the backdrop say the same thing in the same language; AUDIO deliberately does not, and a figure colored by centroid over a spectrogram is not supposed to match it. Only the Takens, stereo and polar embeddings have a trail that is a time axis, so elsewhere both are one flat tint. Displays with a value of their own — the spectrogram, the RTA, the transfer function — do not consult this knob at all, and it dims in those modes."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">src</span><span id="gradient-stack"></span></span></span><button class="rst" id="rst-gradient-source" title="Reset the color source">↺</button><select id="gradient-source" title="Source — what the color follows." style="display:none">
    <option value="5" title="OFF — a flat trace in the start swatch; the color follows nothing at all">off</option>
    <option value="0" title="X — the color follows the figure's X coordinate">X</option>
    <option value="1" title="Y — the color follows the figure's Y coordinate">Y</option>
    <option value="2" selected title="Z — the color follows the figure's Z coordinate">Z</option>
    <option value="3" title="TRAIL — the color follows age along the trail, newest to oldest">trail</option><option value="4" title="AUDIO — the color follows the short-time spectral centroid of the sound: which frequency the moment sits at, not how loud it is">audio</option><option value="6" title="LEVEL — the color follows the short-time level, which is the quantity the spectrogram backdrop paints. Put a colormap palette behind it (heat, turbo, viridis, magma) and the figure and the backdrop agree: loud is the hot end in both">level</option><option value="11" title="dB — LEVEL on a decibel scale, −60 dB to full scale, FIXED: half way up the ramp is always −30 dB, so two moments can be compared. Linear level spends most of its range on the loudest few percent">dB</option><option value="7" title="CORRELATION — how much L and R agree, at each point of the sweep. Fixed scale: the bottom of the ramp is out of phase (what vanishes when summed to mono), the middle uncorrelated, the top identical. A correlation meter says something in here is out of phase; this says WHICH PART">corr</option><option value="8" title="SIDE — the level of (L−R)/2, the width of the image moment by moment. Auto-ranged: it answers which parts are wider than which. Turn WIDE and watch it respond">side</option><option value="9" title="BALANCE — which side the energy is on, hard left to hard right, FIXED with centered at the middle of the ramp. Meant for a diverging palette, where an image drifting off center shows as a color drift">bal</option><option value="13" title="POSITION — the ANGLE of the (L,R) vector: hue IS the stereo position of the sample rather than a quantity computed about it. On a hue palette the figure&#39;s arms take a color per direction">pos</option><option value="10" title="FLUX — how much the spectrum CHANGED from the previous slice, rectified so only increases count. Steady tones go flat and transients flash: onsets painted onto the trace">flux</option><option value="12" title="PITCH — the centroid folded into one octave, so the same note is the same color in any register. Fixed, and least like the others on a hue palette: a melody becomes a sequence of hues that repeats when it does">pitch</option>
  </select></span>
  <span class="pcell axcol rstcell" id="clock-cell" title="Range — whether the color scale refits itself. AUTO continuously stretches the ramp to the loudest and quietest thing currently in view, which keeps a quiet passage visible and means the same color says different things a minute apart. HELD freezes the ends where they are, so color becomes comparable between moments — the difference between a display that looks right and a reading that can be trusted. It has no effect on the sources that are already on a fixed scale (dB, corr, bal, pos, pitch)."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">rng</span><span id="colorlock-stack"></span></span></span><button class="rst" id="rst-color-lock" title="Reset the color range to auto">↺</button><select id="color-lock" title="Color range — auto-ranging or held" style="display:none">
    <option value="0" selected title="AUTO — the ramp refits itself to what is in view">auto</option>
    <option value="1" title="HELD — the ramp stays where it is, so color is comparable between moments">held</option>
  </select></span>
  <span class="pcell axcol rstcell" id="map-cell" title="Map — how a value becomes a color, for every display in the rack including the spectrogram. 2 and 3 mix the Palette module's own swatches; HUE is a raw hue sweep; heat, blue, gray, turbo, viridis and magma are the published colormaps. Turbo and viridis are the even ones — equal steps in the value look like equal steps in color, which a hue sweep does not give you. This is the only colormap control there is: the spectrogram had a second one naming the same six maps, and two knobs that had to be kept in step by hand were one knob too many."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">map</span><span id="map-stack"></span></span></span><button class="rst" id="rst-gradient-colors" title="Reset the color map">↺</button><select id="gradient-colors" title="Map — how a value becomes a color." style="display:none">
    <option value="2" selected title="2-color — a straight mix between the Palette module's start and end swatches">2-color</option>
    <option value="3" title="3-color — the start, middle and end swatches mixed in turn">3-color</option>
    <option value="4" title="hue sweep — a raw sweep round the hue circle: vivid, and not perceptually even">hue sweep</option><option value="5" title="heat — black through red and orange to white">heat</option><option value="6" title="blue — the cool counterpart of heat">blue</option><option value="7" title="gray — luminance alone: the map for reading shape rather than value">gray</option><option value="8" title="turbo — an even rainbow; equal steps in the value look like equal steps in color">turbo</option><option value="9" title="viridis — even, and readable with color-blindness">viridis</option><option value="10" title="magma — even, black through purple and red to cream">magma</option>
  </select></span>
  <span class="pcell axcol vmcell" id="grp-rainbow" title="Palette period — how many times the palette is crossed across the trail (low = a narrow slice of it, which the shift knob then sweeps). Drives the rainbow AND the six colormaps: the question is the same one either way."><span class="punit-top"><span class="plabel">period</span><input type="number" id="slider-value-rfreq" class="numin" title="Palette period value — type or scroll" min="0.05" max="20" step="0.05" value="1"></span><input type="range" id="rainbow-freq" min="0.05" max="20" value="1" step="0.05" title="Palette period — palette crossings across the trail"><button class="rst" id="rst-rfreq" title="Reset palette period">↺</button></span>
  <span class="pcell axcol vmcell" id="grp-pshift" title="Palette shift — where the window sits on the colormap. Turn the period down to a slice and this sweeps that slice along heat / blue / gray / turbo / viridis / magma; route it from audio in the Mod module and the sound moves the figure THROUGH the map instead of only tinting it. Past either end the map turns back rather than wrapping — turbo's two ends are different colors, and joining them puts a hard seam across the figure — so ±1 is the reversed map, not a dead stop."><span class="punit-top"><span class="plabel">shift</span><input type="number" id="slider-value-pshift" class="numin" title="Palette shift value — type or scroll" min="-1" max="1" step="0.01" value="0"></span><input type="range" id="palette-shift" min="-1" max="1" value="0" step="0.01" title="Palette shift — where the colormap window starts"><button class="rst" id="rst-pshift" title="Reset palette shift">↺</button></span>
  <span class="pcell axcol vmcell" id="trail-controls" title="Trail length — how many recent points stay lit"><span class="punit-top"><span class="plabel">Trail</span><input type="number" id="slider-value-trail" class="numin" title="Trail length value (points) — type or scroll" min="1000" max="500000" step="1000" value="20000"></span><input type="range" id="trail-slider" min="1000" max="500000" value="20000" step="1000" title="Trail length — how many recent points stay lit"><button class="rst" id="rst-trail" title="Reset trail length">↺</button></span>
</div></div>
<div class="sect"><div class="sect-hdr" title="Palette — the gradient's start / middle / end colors and the background">Palette</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell pal-cell" id="grp-cstart" title="Gradient START color (low end of the source axis) — pick with the swatch or dial it with the Hue (outer) / Level (inner) knob"><span class="punit-top"><span class="plabel" id="lbl-cstart">start</span><span class="cswatch"><input type="color" id="color-base" value="#ff0000" title="Gradient start color"><button class="rst" id="rst-color-base" title="Reset start color">↺</button></span></span><span class="ckholder" id="ck-color-base"></span></span>
  <span class="pcell axcol vmcell pal-cell" id="grp-cmid" title="Gradient MIDDLE color (3-color palette only) — swatch or Hue / Level knob"><span class="punit-top"><span class="plabel">mid</span><span class="cswatch"><input type="color" id="color-mid" value="#00ff00" title="Gradient middle color"><button class="rst" id="rst-color-mid" title="Reset middle color">↺</button></span></span><span class="ckholder" id="ck-color-mid"></span></span>
  <span class="pcell axcol vmcell pal-cell" id="grp-cend" title="Gradient END color (high end of the source axis) — swatch or Hue / Level knob"><span class="punit-top"><span class="plabel">end</span><span class="cswatch"><input type="color" id="color-top" value="#0000ff" title="Gradient end color"><button class="rst" id="rst-color-top" title="Reset end color">↺</button></span></span><span class="ckholder" id="ck-color-top"></span></span>
  <span class="pcell axcol vmcell pal-cell" id="grp-cbg" title="Background color behind the model — swatch or Hue / Level knob"><span class="punit-top"><span class="plabel">bg</span><span class="cswatch"><input type="color" id="color-bg" value="#000000" title="Background color"><button class="rst" id="rst-color-bg" title="Reset background color">↺</button></span></span><span class="ckholder" id="ck-color-bg"></span></span>
  <span class="pcell axcol swcell" title="Gradient direction — beside the three gradient colors it reverses, rather than in the Console a bay away."><span class="punit-top"><span class="plabel">grad</span></span>
    <label class="swline" style="cursor:pointer;" title="Reverse the color-gradient direction (swap start and end)"><input type="checkbox" class="sw" id="gradient-reverse"> Invert</label>
  </span>
</div></div>
<div class="sect recmodule" id="record-module"><div class="sect-hdr" title="Record — a monitor showing exactly what will be captured, and the transport that captures it. The picture is the recorded area, not the whole canvas, so what you see here is what lands in the file.">Record</div>
<div class="recgrid">
  <div class="rec-screen-unit" data-no-drag title="Monitor — the recorded area, live. Letterboxed, so the shape of the picture is the shape of the region. Timecode and status are burned into the picture the way a field monitor overlays them.">
    <span class="monbezel"><canvas id="rec-preview" width="244" height="230"></canvas><span id="rec-tally" class="rec-tally"></span></span>
  <label class="grp" style="cursor:pointer;" title="Monitor — power to this module's screen. The picture is a copy out of the model's own drawing buffer every frame, which is not free; the rack shows every module in a bay now, so a screen you are not watching needs a switch of its own rather than relying on the module being put away. Off, the glass goes dark and the module costs nothing."><input type="checkbox" class="sw" id="rec-mon-on" checked> Monitor</label>
  </div>
  <div class="rec-controls">
    <div class="rec-unit" title="Format — WebM video (MediaRecorder; small, and plays anywhere a browser does) or animated GIF (256 colors, larger per second, plays anywhere at all). The GIF's palette is built from the clip itself, so gradients do not band.">
      <span class="u-lbl mlbl">format</span>
      <span class="rec-swrow"><span class="rec-legend">webm</span><input type="checkbox" class="sw" id="rec-gif-sw"><span class="rec-legend">gif</span></span>
    </div>
    <div class="rec-unit" title="Area — the whole canvas, or a region you drag on it. Switching to region takes the canvas's drag until you have chosen an area, then gives it back; the monitor shows what you picked.">
      <span class="u-lbl mlbl">area</span>
      <span class="rec-swrow" title="What gets recorded: the full canvas, or a region of it. On region, drag on the canvas to choose the area — that drag picks the region instead of turning the model, and the camera comes back as soon as you let go. The dashed outline stays to show the choice; switch back to full to clear it."><span class="rec-legend">full</span><input type="checkbox" class="sw" id="rec-region-sw"><span class="rec-legend">region</span></span>
    </div>
    <div class="rec-unit rec-transport">
      <span class="u-lbl mlbl">transport</span>
      <span class="grp rec-btns">
        <button class="recbtn recbtn-rec" id="rec-btn" title="Record — starts capturing the area on the monitor. Press again, or Stop, to end the take and save the file."></button>
        <button class="recbtn recbtn-stop" id="rec-stop-btn" title="Stop — ends the take and saves. A GIF is encoded when it stops, which takes a moment for a long clip."></button>
        <button class="recbtn recbtn-still" id="screenshot-btn" title="Still — saves a PNG of what is on the monitor, at full resolution. Honors the area switch, so with a region set it captures the region and not the whole canvas."></button>
      </span>
    </div>
    <div class="rec-unit rec-media" title="Media — how much of a GIF's frame budget the take has used. A GIF is held in memory until it is encoded, so it has a hard ceiling and stops there on its own; WebM streams to disk as it goes and has none.">
      <span class="u-lbl mlbl">media</span>
      <span class="rec-meter"><span class="rec-meter-fill" id="rec-meter-fill"></span></span>
      <span class="rec-counter" id="rec-status">--:--:--</span>
    </div>
    <div class="rec-unit rec-log" title="Last take — what the previous recording came out as. A GIF is encoded when the take ends, so its size is not known until then; this is where it appears.">
      <span class="u-lbl mlbl">last take</span>
      <span class="rec-logline" id="rec-log">no takes yet</span>
    </div>
  </div>
</div>
<input type="checkbox" id="rec-sw" class="rec-hidden-state">
</div>
<div class="sect"><div class="sect-hdr" title="View — orientation: per-axis angle knobs and continuous spin rates">View</div>
<div class="row vmrow vmaxrow">
  <span class="pcell axcol axrot" data-no-drag title="X axis — angle knob, spin rate, horizontal position">
    <span class="plabel">X</span>
    <span class="grp"><span class="knob" id="knob-x" title="X rotation angle — drag or turn to tilt about X"><i class="knob-ptr" id="knobptr-x"></i></span><span class="led" id="led-x" title="X rotation angle in degrees — drag the model or turn the outer ring to change">000°</span></span>
    <span class="grp axsub"><span class="axlbl">rate</span><input type="range" id="rotation-controls-x" min="-1" max="1" value="0" step="0.1" title="X spin rate — continuous rotation about X"><input type="number" id="slider-value-x" class="numin" title="X spin rate value — type or scroll" min="-1" max="1" step="0.1" value="0"><button class="rst" id="rst-rx" title="Reset X angle + spin rate">↺</button></span>
  </span>
  <span class="pcell axcol axrot" data-no-drag title="Y axis — angle knob, spin rate, vertical position">
    <span class="plabel">Y</span>
    <span class="grp"><span class="knob" id="knob-y" title="Y rotation angle — drag or turn to tilt about Y"><i class="knob-ptr" id="knobptr-y"></i></span><span class="led" id="led-y" title="Y rotation angle in degrees — drag the model or turn the outer ring to change">000°</span></span>
    <span class="grp axsub"><span class="axlbl">rate</span><input type="range" id="rotation-controls-y" min="-1" max="1" value="0" step="0.1" title="Y spin rate — continuous rotation about Y"><input type="number" id="slider-value-y" class="numin" title="Y spin rate value — type or scroll" min="-1" max="1" step="0.1" value="0"><button class="rst" id="rst-ry" title="Reset Y angle + spin rate">↺</button></span>
  </span>
  <span class="pcell axcol axrot" data-no-drag title="Z axis — angle knob, spin rate, zoom (depth)">
    <span class="plabel">Z</span>
    <span class="grp"><span class="knob" id="knob-z" title="Z rotation angle — drag near the rim to roll about Z"><i class="knob-ptr" id="knobptr-z"></i></span><span class="led" id="led-z" title="Z rotation angle in degrees — drag near the rim or turn the outer ring to change">000°</span></span>
    <span class="grp axsub"><span class="axlbl">rate</span><input type="range" id="rotation-controls-z" min="-1" max="1" value="0" step="0.1" title="Z spin rate — continuous rotation about Z"><input type="number" id="slider-value-z" class="numin" title="Z spin rate value — type or scroll" min="-1" max="1" step="0.1" value="0"><button class="rst" id="rst-rz" title="Reset Z angle + spin rate">↺</button></span>
  </span>
  <span class="pcell axcol vmcell" data-no-drag title="Fore — where the model sits relative to the rack, as a moving partition rather than a switch. Fully back and all of it is behind the controls; fully forward and all of it is in front; anywhere between and the panel cuts through it, near half in front and far half behind. The cut is a plane parallel to the screen, so it stays put as you turn the model. A flat picture — the spectrogram, the terminal — has no depth to cut, so it moves whole."><span class="punit-top"><span class="plabel">fore</span><input type="number" id="slider-value-fore" class="numin" title="Fore — -1 is entirely behind the rack, +1 entirely in front, 0 cuts through the middle" min="-1" max="1" step="0.05" value="-1"></span><input type="range" id="model-fore" min="-1" max="1" value="-1" step="0.05" title="Fore — slide the model through the rack"><button class="rst" id="rst-fore" title="Reset Fore (back behind the rack)">↺</button></span>
  <span class="pcell axcol swcell" title="Motion — whether the model is moving, and what is moving it. These sit in View because View is where the spin-rate knobs are: Auto-rotate folds straight into the Y rate beside it, and Woodson &amp; Conover's rule is that a control goes close to the display it affects (2nd ed. §2-132)."><span class="punit-top"><span class="plabel">motion</span></span>
    <label class="swline" style="cursor:pointer;" title="Continuously spin the model about the vertical (Y) axis — folds into the Y spin-rate knob beside it"><input type="checkbox" class="sw" id="auto-rotate" checked> Auto-rot</label>
    <label class="swline" style="cursor:pointer;" title="Pause / freeze the animation"><input type="checkbox" class="sw" id="pause-sw"> Pause</label>
    <label class="swline" id="phys-sw-wrap" style="cursor:pointer;display:none;" title="Physics — give the turtle path weight: it becomes a rigid body in the plane of the screen, inside a room whose walls are the edges of the picture. Reveals the Physics module. Press on the figure to pick it up and throw it; press beside it to turn the view as usual."><input type="checkbox" class="sw" id="phys-sw"> Physics</label>
    <label class="swline" style="cursor:pointer;" title="Jam / attract mode — the app performs itself: hops to a random attractor every 12–20 s with fresh gentle spin and occasional persist paint. Never touches speaker outputs."><input type="checkbox" class="sw" id="jam-sw"> Jam</label>
  </span>
</div>
</div>
<div class="sect"><div class="sect-hdr" title="Position — slide the model horizontally / vertically and zoom the camera">Position</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell"><span class="punit-top"><span class="plabel">X</span><input type="number" id="slider-value-panx" class="numin" title="X position value — type or scroll" min="-8" max="8" step="1" value="0"></span><input type="range" id="pan-x" min="-8" max="8" value="0" step="1" title="X position — slide the model horizontally"><button class="rst" id="rst-panx" title="Reset X position">↺</button></span>
  <span class="pcell axcol vmcell"><span class="punit-top"><span class="plabel">Y</span><input type="number" id="slider-value-pany" class="numin" title="Y position value — type or scroll" min="-8" max="8" step="1" value="0"></span><input type="range" id="pan-y" min="-8" max="8" value="0" step="1" title="Y position — slide the model vertically"><button class="rst" id="rst-pany" title="Reset Y position">↺</button></span>
  <span class="pcell axcol vmcell"><span class="punit-top"><span class="plabel">Zoom</span><input type="number" id="slider-value-zoom" class="numin" title="Zoom value — type or scroll; the knob's inner disc trims finer" min="-95" max="95" step="0.25" value="0"></span><input type="range" id="camera-zoom" min="-95" max="95" value="0" step="0.25" title="Zoom — camera distance to the model"><button class="rst" id="rst-zoom" title="Reset zoom">↺</button></span>
</div>
</div>
<div class="sect" id="grid-module"><div class="sect-hdr" title="Grid — how many views of the model are drawn, what differs between them, and which of them the knobs are pointed at. It sits in the DISPLAY bay beside View and Position because it is a question about the picture, not about the signal: Trace is how one trajectory is drawn, View is where the camera is, and this is how many cameras there are.">Grid</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell" data-no-drag title="Grid — how many views of the model are drawn, as a square (2 is side by side). A grid is not that many instruments configured by hand: pick what varies with the Sweep dial and the cells draw that parameter's range, which is a contact sheet of one setting rather than sixteen panels to fill in."><span class="punit-top"><span class="plabel">grid</span></span><span class="grp vmbay"><span id="view-n-stack"></span></span><select id="view-n" style="display:none">
      <option value="0" selected title="1 — a single view">1</option>
      <option value="1" title="2 — side by side">2</option>
      <option value="2" title="4 — two by two">4</option>
      <option value="3" title="9 — three by three">9</option>
      <option value="4" title="16 — four by four">16</option>
    </select></span>
  <span class="pcell axcol vmcell" data-no-drag title="Sweep — which parameter varies across the grid, and the from and to knobs beside this one say over how much of its range. This is what makes a grid of sixteen usable: rather than sixteen panels to fill in, one dial says what differs and the cells draw that parameter's space, like a contact sheet. The list is the CURRENT model's own parameters, so it changes with the mode; a model that integrates its trajectory offers only the colorings, because one pass of it continues where the last left off and a swept grid would smear one trajectory across the cells rather than show one per cell. csrc and cmap sweep the COLORING instead of a number — the same figure read nine different ways, or in nine palettes — which is the case a grid is most worth having for."><span class="punit-top"><span class="plabel">sweep</span></span><span class="grp vmbay"><span id="sweep-p-stack"></span></span><select id="sweep-p" style="display:none">
      <!-- Filled by buildSweepDial: the targets are the CURRENT model's own parameters, so this list changes with the mode. -->
      <option value="0" selected title="none — every cell the same">—</option>
    </select></span>
  <span class="pcell axcol vmcell" data-no-drag title="Sweep down — a SECOND parameter, varying down the grid while the first varies across it. This is what the second direction of a grid is for: with one sweep a three by three of nine delays wasted its rows, and asking for nine palettes meant giving up the nine delays. Set both and each column is one value of across, each row one value of down, and the cell where they meet is the pair — nine delays read in three palettes, say. Set only this one and it behaves exactly like the sweep beside it. The two cannot name the same parameter: rows and columns varying the same thing is not a comparison."><span class="punit-top"><span class="plabel">down</span></span><span class="grp vmbay"><span id="sweep2-p-stack"></span></span><select id="sweep2-p" style="display:none">
      <!-- Filled by buildSweepDial from the same per-mode target lists as sweep-p. -->
      <option value="0" selected title="none — the grid varies in one direction only">—</option>
    </select></span>
  <span class="pcell axcol vmcell" id="sweep-lo-cell" title="Sweep from — where in the swept parameter's own range the first cell sits, as a fraction: 0 is the parameter's minimum. Turn both this and to inward and the contact sheet covers a slice of the range in detail rather than all of it coarsely, which is how a sweep is used once you know roughly where the interesting part is."><span class="punit-top"><span class="plabel">from</span><input type="number" id="slider-value-swlo" class="numin" title="Sweep start value — type or scroll" min="0" max="1" step="0.01" value="0"></span><input type="range" id="sweep-lo" min="0" max="1" value="0" step="0.01" title="Sweep start, as a fraction of the parameter's range"><button class="rst" id="rst-swlo" title="Reset sweep start">↺</button></span>
  <span class="pcell axcol vmcell" id="sweep-hi-cell" title="Sweep to — where in the swept parameter's own range the last cell sits, as a fraction: 1 is the parameter's maximum. Below the from knob and the sweep runs backwards, which is worth having when the interesting end should be read first."><span class="punit-top"><span class="plabel">to</span><input type="number" id="slider-value-swhi" class="numin" title="Sweep end value — type or scroll" min="0" max="1" step="0.01" value="1"></span><input type="range" id="sweep-hi" min="0" max="1" value="1" step="0.01" title="Sweep end, as a fraction of the parameter's range"><button class="rst" id="rst-swhi" title="Reset sweep end">↺</button></span>
  <span class="pcell axcol vmcell" data-no-drag id="focus-n-cell" title="Focus — which cell of the grid the knobs drive while Link is off. Its positions follow the grid, so a sixteen-cell sheet has sixteen of them. Moving it rebuilds the parameter rows against that cell's own instance, which is what makes a knob write to the view you are looking at rather than to the one behind it."><span class="punit-top"><span class="plabel">focus</span></span><span class="grp vmbay"><span id="focus-n-stack"></span></span><label class="swline" style="cursor:pointer;" title="Link — the two views share one set of knobs. On (the default) the panel drives both halves, which is what to use for comparing two colorings or two camera angles of one setting. Off, each half keeps its own parameters and the panel drives whichever the focus dial has selected — which is what to use for comparing two settings of the instrument."><input type="checkbox" class="sw" id="link-sw" checked> Link</label><select id="focus-n" style="display:none">
      <!-- Filled by buildFocusDial: one position per cell of the CURRENT grid. -->
      <option value="0" selected title="A — the first cell">A</option>
    </select></span>
</div>
</div>
<div class="sect"><div class="sect-hdr" title="Display — animation speed, line width, and the knob step / fine multipliers">Display</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell"><span class="punit-top"><span class="plabel">Speed</span><input type="number" id="slider-value-speed" class="numin" title="Speed value ×0.01–×100 — type or scroll" min="0.01" max="100" step="0.01" value="1"></span><input type="range" id="speed-slider" min="-2" max="2" value="0" step="0.1" title="Speed — animation rate (sub-steps / dt scale)"><button class="rst" id="rst-speed" title="Reset speed">↺</button></span>
  <span class="pcell axcol vmcell"><span class="punit-top"><span class="plabel">Line</span><input type="number" id="slider-value-line" class="numin" title="Line width value — type or scroll" min="1" max="10" step="1" value="1"></span><input type="range" id="line-width" min="1" max="10" value="1" step="1" title="Line width — trace thickness"><button class="rst" id="rst-line" title="Reset line width">↺</button></span><span class="pcell axcol vmcell"><span class="punit-top"><span class="plabel">Points</span><input type="number" id="slider-value-dash" class="numin" title="Point count — type or scroll; 0 is the solid line" min="0" max="4000" step="10" value="0"></span><input type="range" id="dash-duty" min="0" max="4000" value="0" step="10" title="Points — how many points the line breaks into. 0 (and anything past the trail's own vertex count) draws it solid, exactly as it always has; lower it and the line becomes that many points, further apart as the number falls. Each point is one vertex wide, so the continuity at the top is not a separate setting — it is what happens when there is a point for every vertex. Useful on a figure that folds over itself, where points read and a solid line becomes a thicket."><button class="rst" id="rst-dash" title="Reset points (0 = the solid line)">↺</button></span>
  <span class="pcell axcol vmcell sf-cell" id="stepfine-grp" title="Outer ring = Step× (coarse step), inner ring = Fine× (fraction of a step)"><span class="sf-hdr"><span class="sf-row"><span class="plabel">step</span><span class="led sf-led" id="step-led" title="current Step×">1</span></span></span><span class="grp vmbay"><span id="stepfine-stack"></span></span><span class="sf-ftr"><span class="sf-row"><span class="plabel">fine</span><span class="led sf-led" id="fine-led" title="current Fine×">.1</span></span></span><button class="rst" id="rst-stepfine" title="Reset the step and fine multipliers">↺</button><select id="step-ratio" title="Step multiplier — scales every knob’s coarse step (outer ring)" style="display:none"><option value="0.25" title="0.25x — a quarter of each knob's normal coarse step">0.25</option><option value="0.5" title="0.5x — half of each knob's normal coarse step">0.5</option><option value="1" selected title="1x — each knob's normal coarse step">1</option><option value="2" title="2x — twice each knob's normal coarse step">2</option><option value="5" title="5x — five times each knob's normal coarse step">5</option></select><select id="fine-ratio" title="Fine multiplier — the fine-trim disc’s fraction of a step (inner knob)" style="display:none"><option value="1" title="1 — the fine disc steps as far as the coarse ring does">1</option><option value="0.1" selected title="0.1 — the fine disc steps a tenth of a coarse step">0.1</option><option value="0.01" title="0.01 — the fine disc steps a hundredth of a coarse step">0.01</option><option value="0.001" title="0.001 — the fine disc steps a thousandth of a coarse step">0.001</option></select></span>
  <span class="pcell axcol swcell" title="Trace — how the trajectory itself is drawn. In Display rather than the Console because the knobs it is read against — speed, line width — are the two cells beside it."><span class="punit-top"><span class="plabel">trace</span></span>
    <label class="swline" style="cursor:pointer;" title="Draw the trajectory as discrete points instead of a connected line"><input type="checkbox" class="sw" id="use-points"> Points</label>
    <label class="swline" style="cursor:pointer;" title="Keep the entire trail on screen (never clear old points) — accumulates the full attractor"><input type="checkbox" class="sw" id="persist-trail"> Persist</label>
    <label class="swline" style="cursor:pointer;" title="Ring trail — scope-style beam: only the advancing head integrates each frame (the trail is its history), so knob/audio changes bend the path from the head forward instead of reshaping the whole curve, and long trails cost almost nothing. Off = classic scan (whole curve recomputed and reshaped every frame)."><input type="checkbox" class="sw" id="ring-sw"> Ring</label>
  </span>
  <span class="pcell axcol swcell" title="Overlay — what else is drawn on the face besides the trajectory: a second copy of the flow, the section it pierces, and the ruled graticule a reading is taken against."><span class="punit-top"><span class="plabel">overlay</span></span>
    <label class="swline" style="cursor:pointer;" title="Twin trajectories — a second copy of the flow starts ε apart (green) and the two visibly separate at the attractor's own Lyapunov rate; the λ readout is a live largest-Lyapunov-exponent estimate (positive = chaotic). Both copies use the same integrator, so what separates them is the dynamics."><input type="checkbox" class="sw" id="twin-sw"> Twin <span class="led" id="twin-lambda" title="Live largest Lyapunov exponent estimate — positive means chaos (e-folding rate of the separation per time unit)"></span></label>
    <label class="swline" style="cursor:pointer;" title="Poincaré section — sample the trajectory only where it pierces a plane, going one way through it, and draw the accumulated intersections in gold: the flow's sheets collapse into the section's fractal scatter. The crossing is interpolated between the two samples that straddle the plane rather than snapped to the nearer one, which would smear the section by up to half a step of arc. Reveals the Section module, where the plane is placed and its direction chosen. Analysis &#8594; Poincar&#233; Section is the same section as a picture of its own, with the first-return map."><input type="checkbox" class="sw" id="sect-sw"> Sect</label>
    <label class="swline" style="cursor:pointer;" title="Graticule — the ruled face behind a scope trace: eight divisions by ten, the two center axes cut heavier, minor ticks every fifth of a division so a reading can be interpolated, and the 0%/10%/90%/100% marks a risetime is measured between. It is what makes a trace a MEASUREMENT rather than a picture of one, so it is on by default — turn it off for a clean screen. It only has anything to draw where the CRT look does: the scope models, or any model with the CRT color source."><input type="checkbox" class="sw" id="scope-grat" checked> Grat</label>
  </span>
</div></div>
<div class="sect"><div class="sect-hdr" title="Style — interface size + rack style, knob face + LED color, and CRT phosphor">Style</div>
<div class="row vmrow">
  <span class="pcell axcol rstcell" title="Outer ring = size of every knob (S / M / L / XL), which scales the whole control interface; inner ring = the rack's metalwork when the Rack bay is on (bare / handles / screws / both)."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">Size</span><span id="size-stack"></span></span></span><button class="rst" id="rst-knob-size" title="Reset the interface size and the rack style">↺</button><select id="knob-size" title="Interface size — scales every knob and readout (S / M / L / XL)" style="display:none"><option value="0.85" title="S — small: every knob and readout at 85%">S</option><option value="1" selected title="M — medium: the standard size">M</option><option value="1.3" title="L — large: every knob and readout at 130%">L</option><option value="1.7" title="XL — extra large: every knob and readout at 170%">XL</option></select><select id="bay-style" title="Rack style — the frame's metalwork when the Rack bay is on (inner ring)" style="display:none"><option value="bare" title="bare — rails and ears with their bolt holes, nothing more">bare</option><option value="handles" title="handles — a grab handle on each ear">hndl</option><option value="screws" title="screws — every module screwed to the rails">scrw</option><option value="full" selected title="full — grab handles on the ears and every module screwed in">full</option></select></span>
  <span class="pcell axcol rstcell" title="Outer ring = knob appearance (std / flat / vint / chrome / gold / carbon); inner ring = LED readout color."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">Knob</span><span id="knobstyle-stack"></span></span></span><button class="rst" id="rst-knob-style" title="Reset the knob style and LED color">↺</button><select id="knob-style" title="Knob appearance — face finish (outer ring: std / flat / vint / chrome / gold / carbon)" style="display:none"><option value="std" selected title="std — the standard knob face">std</option><option value="flat" title="flat — a matte face with no highlight">flat</option><option value="vint" title="vint — a vintage cream face">vint</option><option value="chrome" title="chrome — a polished metal face">chrome</option><option value="gold" title="gold — a brushed gold face">gold</option><option value="carbon" title="carbon — a carbon-fiber face">carbon</option></select><select id="led-color" title="LED color — readout hue for every LED display (inner knob)" style="display:none"></select></span>
  <span class="pcell axcol rstcell" id="phosphor-cell" title="CRT phosphor for scope traces (Lissajous / Graphic Artist) — sets trace color + afterglow. P31 crisp green … P7 blue→green … P33 long amber."><span class="grp vmbay"><span class="grp knoblbl"><span class="plabel">Phosphor</span><span id="phosphor-stack"></span></span></span><button class="rst" id="rst-phosphor" title="Reset the phosphor to off">↺</button><select id="phosphor" title="Phosphor — CRT trace color and afterglow for scope modes" style="display:none"></select></span>
</div>
</div>
<div class="sect gen-osc" id="gen-x-module"><div class="sect-hdr" title="Oscillator X — the xy scope's horizontal axis. freq knob (log, equal turn per octave), level knob, and a dual knob: outer ring = speaker channel, inner = waveform.">Gen X</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">freq</span><input type="number" class="numin gen-led" id="gen-x-led" title="Gen X frequency in Hz — type a value or turn the knob (log scale, one turn per octave)" min="27.5" max="28160" step="1" value="200"></span><input type="range" id="gen-x-freq" min="0" max="120" step="1" value="34" style="display:none"><span class="grp vmbay"><span id="gen-x-fstack"></span></span><button class="rst" id="rst-gen-x-freq" title="Reset the frequency">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="gen-x-lvl-led" title="Gen X level (0–100)" min="0" max="100" step="1" value="80"></span><input type="range" id="gen-x-lvl" min="0" max="100" step="1" value="80" style="display:none"><span class="grp vmbay"><span id="gen-x-lstack"></span></span><button class="rst" id="rst-gen-x-lvl" title="Reset the level">↺</button></span>
  <span class="pcell axcol vmcell gen-cell gen-out-cell"><span class="punit-top"><span class="plabel">out</span></span><span class="grp vmbay"><span id="gen-x-ostack"></span></span><button class="rst" id="rst-gen-x-out" title="Reset the X output routing and waveform">↺</button><select id="gen-x-out" title="X output — speaker channel routing (outer ring: off / L / R / L+R)" style="display:none"><option value="off" selected title="off — Gen X is muted">off</option><option value="l" title="L — Gen X plays the left channel only">L</option><option value="r" title="R — Gen X plays the right channel only">R</option><option value="both" title="L+R — Gen X plays both channels">L+R</option></select><select id="gen-x-wave" title="X waveform — oscillator shape (inner knob: sine / tri / sqr / saw / noise)" style="display:none"><option value="0" title="sine — one pure frequency and no harmonics at all">sine</option><option value="1" title="triangle — odd harmonics falling away steeply: soft and hollow">tri</option><option value="2" title="square — odd harmonics falling away slowly: hollow and reedy">sqr</option><option value="3" title="sawtooth — every harmonic, the brightest of the four">saw</option><option value="4" title="noise — a 15-bit shift register; the freq knob sets its playback rate">noise</option></select></span>
</div></div>
<div class="sect gen-osc" id="gen-y-module"><div class="sect-hdr" title="Oscillator Y — the xy scope's vertical axis. freq knob (log), level knob, and a dual knob: outer ring = speaker channel, inner = waveform.">Gen Y</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">freq</span><input type="number" class="numin gen-led" id="gen-y-led" title="Gen Y frequency in Hz — type a value or turn the knob (log scale, one turn per octave)" min="27.5" max="28160" step="1" value="300"></span><input type="range" id="gen-y-freq" min="0" max="120" step="1" value="41" style="display:none"><span class="grp vmbay"><span id="gen-y-fstack"></span></span><button class="rst" id="rst-gen-y-freq" title="Reset the frequency">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="gen-y-lvl-led" title="Gen Y level (0–100)" min="0" max="100" step="1" value="80"></span><input type="range" id="gen-y-lvl" min="0" max="100" step="1" value="80" style="display:none"><span class="grp vmbay"><span id="gen-y-lstack"></span></span><button class="rst" id="rst-gen-y-lvl" title="Reset the level">↺</button></span>
  <span class="pcell axcol vmcell gen-cell gen-out-cell"><span class="punit-top"><span class="plabel">out</span></span><span class="grp vmbay"><span id="gen-y-ostack"></span></span><button class="rst" id="rst-gen-y-out" title="Reset the Y output routing and waveform">↺</button><select id="gen-y-out" title="Y output — speaker channel routing (outer ring: off / L / R / L+R)" style="display:none"><option value="off" selected title="off — Gen Y is muted">off</option><option value="l" title="L — Gen Y plays the left channel only">L</option><option value="r" title="R — Gen Y plays the right channel only">R</option><option value="both" title="L+R — Gen Y plays both channels">L+R</option></select><select id="gen-y-wave" title="Y waveform — oscillator shape (inner knob: sine / tri / sqr / saw / noise)" style="display:none"><option value="0" title="sine — one pure frequency and no harmonics at all">sine</option><option value="1" title="triangle — odd harmonics falling away steeply: soft and hollow">tri</option><option value="2" title="square — odd harmonics falling away slowly: hollow and reedy">sqr</option><option value="3" title="sawtooth — every harmonic, the brightest of the four">saw</option><option value="4" title="noise — a 15-bit shift register; the freq knob sets its playback rate">noise</option></select></span>
</div></div>
<div class="sect gen-osc" id="gen-z-module"><div class="sect-hdr" title="Oscillator Z — a third generator (audio / modulation). freq knob (log), level knob, and a dual knob: outer ring = speaker channel, inner = waveform.">Gen Z</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">freq</span><input type="number" class="numin gen-led" id="gen-z-led" title="Gen Z frequency in Hz — type a value or turn the knob (log scale, one turn per octave)" min="27.5" max="28160" step="1" value="150"></span><input type="range" id="gen-z-freq" min="0" max="120" step="1" value="29" style="display:none"><span class="grp vmbay"><span id="gen-z-fstack"></span></span><button class="rst" id="rst-gen-z-freq" title="Reset the frequency">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="gen-z-lvl-led" title="Gen Z level (0–100)" min="0" max="100" step="1" value="80"></span><input type="range" id="gen-z-lvl" min="0" max="100" step="1" value="80" style="display:none"><span class="grp vmbay"><span id="gen-z-lstack"></span></span><button class="rst" id="rst-gen-z-lvl" title="Reset the level">↺</button></span>
  <span class="pcell axcol vmcell gen-cell gen-out-cell"><span class="punit-top"><span class="plabel">out</span></span><span class="grp vmbay"><span id="gen-z-ostack"></span></span><button class="rst" id="rst-gen-z-out" title="Reset the Z output routing and waveform">↺</button><select id="gen-z-out" title="Z output — speaker channel routing (outer ring: off / L / R / L+R)" style="display:none"><option value="off" selected title="off — Gen Z is muted">off</option><option value="l" title="L — Gen Z plays the left channel only">L</option><option value="r" title="R — Gen Z plays the right channel only">R</option><option value="both" title="L+R — Gen Z plays both channels">L+R</option></select><select id="gen-z-wave" title="Z waveform — oscillator shape (inner knob: sine / tri / sqr / saw / noise)" style="display:none"><option value="0" title="sine — one pure frequency and no harmonics at all">sine</option><option value="1" title="triangle — odd harmonics falling away steeply: soft and hollow">tri</option><option value="2" title="square — odd harmonics falling away slowly: hollow and reedy">sqr</option><option value="3" title="sawtooth — every harmonic, the brightest of the four">saw</option><option value="4" title="noise — a 15-bit shift register; the freq knob sets its playback rate">noise</option></select></span>
</div></div>
<div class="sect gen-osc" id="gen-env-module"><div class="sect-hdr" title="Envelope — the Complex Sound Generator's shaper for the signal generator's speaker output: in RPT mode it cycles attack → decay continuously, a shaped tremolo over whatever Gen X/Y/Z are routed to the speakers. OFF passes the generators through untouched. Analysis paths (scope, spectrogram, meters) stay unshaped.">Envelope</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">atk</span><input type="number" class="numin gen-led" id="gen-env-atk-led" title="Envelope attack time in milliseconds — the rise from silence to full level" min="1" max="2000" step="1" value="10"></span><input type="range" id="gen-env-atk" min="1" max="2000" step="1" value="10" style="display:none"><span class="grp vmbay"><span id="gen-env-astack"></span></span><button class="rst" id="rst-gen-env-atk" title="Reset the envelope attack">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">dcy</span><input type="number" class="numin gen-led" id="gen-env-dcy-led" title="Envelope decay time in milliseconds — the fall from full level back to silence" min="1" max="5000" step="1" value="300"></span><input type="range" id="gen-env-dcy" min="1" max="5000" step="1" value="300" style="display:none"><span class="grp vmbay"><span id="gen-env-dstack"></span></span><button class="rst" id="rst-gen-env-dcy" title="Reset the envelope decay">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">mode</span></span><span class="grp vmbay"><span id="gen-env-mstack"></span></span><button class="rst" id="rst-gen-env-mode" title="Reset the envelope mode">↺</button><select id="gen-env-mode" title="Envelope mode — OFF: generators pass through untouched · RPT: attack → decay repeats continuously (shaped tremolo)" style="display:none"><option value="off" selected title="off — the generators pass through the envelope untouched">off</option><option value="rpt" title="rpt — attack then decay, over and over: a shaped tremolo">rpt</option></select></span>
</div></div>
<div class="sect gen-osc" id="testsig-module"><div class="sect-hdr" title="Test — the stimulus library: the signals a test record carries, synthesized. Pink noise for a spectrum or a room, a logarithmic sweep for an impulse response, 1 kHz as a level and distortion reference, 3150 Hz for wow and flutter, left-only and right-only to find out which speaker is which, a polarity pulse whose direction is visible, and three noises whose channels are identical, independent or inverted — correlation +1, 0 and −1 to check a phase display against. It replaces the X/Y/Z oscillators while it is on; they keep their settings and come back when it is off. Needs Signal gen switched on in the Console.">Test</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">sig</span></span><span class="grp vmbay"><span id="testsig-stack"></span></span><button class="rst" id="rst-testsig-sel" title="Reset the test signal">↺</button><select id="testsig-sel" title="Test signal" style="display:none"></select></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="testsig-lvl-led" title="Test signal level (0–100). 100 is full scale: a reference tone there reads 0 dBFS." min="0" max="100" step="1" value="50"></span><input type="range" id="testsig-lvl" min="0" max="100" step="1" value="50" style="display:none"><span class="grp vmbay"><span id="testsig-lstack"></span></span><button class="rst" id="rst-testsig-lvl" title="Reset the test-signal level">↺</button></span>
  <span class="pcell axcol swcell" title="Source — the two client-side stimulus sources, switched on where their own controls are. Signal gen runs the X/Y/Z oscillators; Test tone plays a sweeping tone out through the speakers and back in through the capture, which is how the loop is exercised with no instrument in it."><span class="punit-top"><span class="plabel">src</span></span>
    <label class="swline" style="cursor:pointer;" title="Signal generator — a built-in client-side audio source (three X/Y/Z oscillators). Drives audio modulation, the spectrogram and the xy scope with no server or microphone, and can play over the speakers. This module's signals replace the oscillators while it is on."><input type="checkbox" class="sw" id="fg-on"> Signal gen</label>
  </span>
</div></div>
<div class="sect gen-osc" id="sonify-module"><div class="sect-hdr" title="Model Out — HEAR the attractor. Inner knob picks how: FLOW integrates the attractor's own equations at audio rate, so the pitch is the system's natural orbital frequency (chaos chirps, periodic windows lock to tones, parameter changes are audible) and RATE transposes it (A4 = ×1); SCAN traces the drawn trail as one waveform period at exactly RATE Hz (a stable, playable tone). MAP picks which two coordinates drive L/R (CAM = the screen's x/y, so rotating the model changes the sound; off = silent). LVL is output level.">Model Out</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">rate</span><input type="number" class="numin gen-led" id="sonify-led" title="Model Out RATE — FLOW: transposition in Hz (440 = ×1) · SCAN: trail sweeps per second" min="27.5" max="28160" step="1" value="110"></span><input type="range" id="sonify-freq" min="0" max="120" step="1" value="24" style="display:none"><span class="grp vmbay"><span id="sonify-fstack"></span></span><button class="rst" id="rst-sonify-freq" title="Reset Model Out rate">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="sonify-lvl-led" title="Model Out level (0–100)" min="0" max="100" step="1" value="60"></span><input type="range" id="sonify-lvl" min="0" max="100" step="1" value="60" style="display:none"><span class="grp vmbay"><span id="sonify-lstack"></span></span><button class="rst" id="rst-sonify-lvl" title="Reset Model Out level">↺</button></span>
  <span class="pcell axcol vmcell gen-cell gen-out-cell"><span class="punit-top"><span class="plabel">map</span></span><span class="grp vmbay"><span id="sonify-mstack"></span></span><button class="rst" id="rst-sonify-map" title="Reset the Model Out mapping and mode">↺</button><select id="sonify-map" title="Model Out mapping (outer ring) — which two model coordinates drive L/R (off = silent)" style="display:none"><option value="off" selected title="off — Model Out is silent">off</option><option value="cam" title="CAM — the two screen axes, as you are looking at the model, drive L and R">CAM</option><option value="xy" title="XY — the model's X drives the left channel and Y the right">XY</option><option value="xz" title="XZ — the model's X drives the left channel and Z the right">XZ</option><option value="yz" title="YZ — the model's Y drives the left channel and Z the right">YZ</option></select><select id="sonify-mode" title="Model Out mode (inner knob) — FLOW: audify the dynamics (pitch emergent, RATE transposes) · SCAN: trail as wavetable (RATE = exact Hz)" style="display:none"><option value="flow" selected title="FLOW — audify the dynamics: the pitch emerges from the system and RATE transposes it">FLOW</option><option value="scan" title="SCAN — read the trail as a wavetable; RATE is then an exact frequency in hertz">SCAN</option></select></span>
</div></div>
<div class="sect gen-osc" id="analysis-module"><div class="sect-hdr" title="Analysis — the largest Lyapunov exponent of the model on screen: how fast two nearby trajectories separate. Positive means chaotic (prediction has a horizon); about zero means periodic or quasi-periodic; negative means the orbit is settling. Flows are per unit time, maps per iterate. Measured on demand, not per frame.">Analysis</div>
<div class="row vmrow meterrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lyap</span><span class="led counter-led" id="lyap-led" title="Largest Lyapunov exponent. /t = per unit time (flows), /n = per iterate (maps).">  --.--</span></span><span class="grp vmbay"><span class="lyap-verdict" id="lyap-verdict" title="Plain reading of the exponent"></span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">meas</span></span><span class="grp vmbay"><button class="rst" id="lyap-remeasure" title="Re-measure now. A run is a few hundred thousand integration steps, so it is done on demand rather than every frame.">↻</button></span></span>
</div></div>
<div class="sect gen-osc" id="preset-module"><div class="sect-hdr" title="Presets — the current view, saved under a name. A preset holds everything the permalink holds: the model, every knob and color, the effect switches, the parameters and the pose. Recalling one resets to defaults and re-applies it, exactly as the Patchbay's numbered patch memories do; the difference is that these have names, and the address bar does not have to carry them.">Presets</div>
<div class="row vmrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">name</span></span><input type="text" id="preset-name" class="stext-in preset-in" maxlength="24" data-no-drag title="Preset name — what to file the current view under. Saving over a name that already exists replaces it; leaving this empty files the view under the current model's name."><span class="grp btn-row demo-btn"><button class="pushbtn" id="preset-save" title="Save the current view under the name beside it — the same complete state a permalink carries"></button><span class="btn-lbl">Save</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">saved</span></span><span class="grp vmbay"><select id="preset-list" class="stl-builtin preset-list" data-no-drag title="Presets saved in this browser"><option value="">— none saved —</option></select></span><span class="grp btn-row demo-btn"><button class="pushbtn" id="preset-recall" title="Recall the selected preset — resets to defaults and re-applies the saved view, and updates the address bar so it is immediately shareable"></button><span class="btn-lbl">Recall</span></span><span class="grp btn-row demo-btn"><button class="pushbtn" id="preset-del" title="Delete the selected preset"></button><span class="btn-lbl">Delete</span></span></span>
</div></div>
<div class="sect gen-osc" id="counter-module"><div class="sect-hdr" title="Counter — a frequency counter for the rack, in the spirit of the glensstuff.com NAND-gate counter: it counts trigger crossings of the live audio source over the gate window and shows cycles per second, the way the discrete-logic original did. Feed it the mic, the ws stream, or the signal generator.">Counter</div>
<div class="row vmrow meterrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">freq</span><span class="led counter-led" id="counter-led" title="Measured frequency in Hz — cycles counted over the last closed gate window">-----.-</span></span><span class="grp vmbay"><span class="counter-gate" id="counter-gate" title="Gate lamp — flashes each time the gate window closes and the readout updates"></span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">gate</span></span><span class="grp vmbay"><span id="counter-gstack"></span></span><button class="rst" id="rst-counter-gate" title="Reset the gate time">↺</button><select id="counter-gatesel" title="Gate time in seconds — how long the counter counts before updating (longer gate = finer resolution, slower updates)" style="display:none"><option value="0.1" title="0.1 s gate — the fastest update and the coarsest resolution">0.1</option><option value="0.5" title="0.5 s gate — twice a second">0.5</option><option value="1" selected title="1 s gate — the standard frequency-counter gate: the reading is hertz directly">1</option><option value="2" title="2 s gate — the slowest update and the finest resolution">2</option></select></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">trig</span><input type="number" class="numin gen-led" id="counter-trig-led" title="Trigger level (% of full scale) — the hysteresis threshold a cycle must swing across to count; raise it to reject noise" min="0" max="30" step="1" value="4"></span><input type="range" id="counter-trig" min="0" max="30" step="1" value="4" style="display:none"><span class="grp vmbay"><span id="counter-tstack"></span></span><button class="rst" id="rst-counter-trig" title="Reset the trigger level">↺</button></span>
</div></div>
<div class="sect gen-osc" id="thd-module"><div class="sect-hdr" title="Distortion — THD, THD+N, SINAD and ENOB of the live audio, measured against whichever tone is in it. Feed it the Test module's 1 kHz reference through whatever you want measured and read how much of what comes back is not that tone. THD is the harmonics alone, which is the number a specification quotes; THD+N is everything that is not the fundamental, which is the honest one and always the larger; SINAD is the same ratio in decibels, the way a converter is specified; ENOB runs the ideal-converter relation backwards and says how many bits would sound this clean. The gap between THD and THD+N is how much of the rubbish is hiss rather than distortion. The instrument's own floor is about 0.0015% THD+N (96.5 dB SINAD, 17.3 effective bits), measured on a synthesized pure tone — that is the Blackman-Harris window's leakage past the notch, and nothing quieter than it can be read.">Distortion</div>
<div class="row vmrow meterrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">thd</span><span class="led counter-led" id="thd-led" title="Total harmonic distortion — the harmonics alone, as a percentage of the fundamental. Noise is not counted, which is what makes this the flattering number.">  --.---</span></span><span class="grp vmbay"><span class="ledlbl">fund</span><span class="led counter-led" id="thd-fund-led" title="The fundamental the measurement is against, in Hz — the largest peak above DC, found between bins">-----.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">thd+n</span><span class="led counter-led" id="thdn-led" title="THD+N — everything that is not the fundamental, as a percentage of it: harmonics, noise, hum, the lot. Always at least THD.">  --.---</span></span><span class="grp vmbay"><span class="ledlbl">lvl</span><span class="led counter-led" id="thd-level-led" title="Level of the fundamental in dBFS. A distortion figure is quoted at a stated level, and near full scale is where it is usually worst.">  --.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">sinad</span><span class="led counter-led" id="thd-sinad-led" title="SINAD in dB — signal-plus-noise-and-distortion over noise-and-distortion, the way a converter is specified.">  --.-</span></span><span class="grp vmbay"><span class="ledlbl">enob</span><span class="led counter-led" id="thd-enob-led" title="Effective number of bits: (SINAD − 1.76) / 6.02. What an ideal converter would have to be to sound this clean.">--.--</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">src</span></span><span class="grp vmbay"><span id="thd-chanstack"></span></span><button class="rst" id="rst-thd-chan" title="Reset the measured channel">↺</button><select id="thd-chan" title="Which signal is measured" style="display:none"></select></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">harm</span><input type="number" class="numin gen-led" id="thd-harm-led" title="How many harmonics are summed into THD (2–20). Harmonics past Nyquist are never counted: they are not there." min="2" max="20" step="1" value="10"></span><input type="range" id="thd-harm" min="2" max="20" step="1" value="10" style="display:none"><span class="grp vmbay"><span id="thd-hstack"></span></span><button class="rst" id="rst-thd-harm" title="Reset the harmonic count">↺</button></span>
</div></div>
<div class="sect gen-osc" id="lufs-module"><div class="sect-hdr" title="Loudness — LUFS to ITU-R BS.1770 and EBU R 128, the scale everything is delivered against. M is the momentary loudness over 400 ms, S the short-term over 3 s, I the INTEGRATED reading over everything since the last reset — gated, so silence and quiet passages do not drag a programme's number down, which is what makes it the number a delivery spec means. LRA is the loudness range, how far the loud parts sit above the quiet ones: one number for how dynamic the material is, and the thing a loudness target alone says nothing about. TP is the TRUE peak in dBTP, the peak of the reconstructed signal rather than of the samples — a full-scale tone at a quarter of the sample rate can have every sample at −3 dBFS and still clip the converter, which is why a delivery ceiling is a true-peak ceiling. K-weighting is derived from the analog prototype at whatever rate the audio is arriving at, not the 48 kHz coefficients the standard prints, so the weighting is the same filter at every rate.">Loudness</div>
<div class="row vmrow meterrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">M</span><span class="led counter-led" id="lufs-m-led" title="Momentary loudness, LUFS over 400 ms — ungated. What it sounds like right now.">  --.-</span></span><span class="grp vmbay"><span class="ledlbl">S</span><span class="led counter-led" id="lufs-s-led" title="Short-term loudness, LUFS over 3 s — the window a person actually judges level over.">  --.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">I</span><span class="led counter-led" id="lufs-i-led" title="Integrated loudness, LUFS over everything since the last reset, gated. The number a delivery spec means.">  --.-</span></span><span class="grp vmbay"><span class="ledlbl">LRA</span><span class="led counter-led" id="lufs-lra-led" title="Loudness range in LU: the 95th percentile of the short-term distribution less the 10th. Percentiles rather than extremes, so one cymbal does not set the range of a whole program.">  --.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">TP</span><span class="led counter-led" id="lufs-tp-led" title="True peak in dBTP — the peak of the reconstructed signal, found by oversampling four times. Higher than the sample peak whenever the waveform crests between samples.">  --.-</span></span><span class="grp vmbay"><button class="rst" id="lufs-reset" title="Start a new integrated measurement. The integrated reading and the loudness range are over everything since this was last pressed; M and S are windows and are not affected.">↺</button></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">tgt</span><input type="number" class="numin gen-led" id="lufs-target-led" title="Delivery target in LUFS — the readout beside it shows how far the integrated reading is from it. −23 is EBU R 128, −14 is where most streaming sits, −16 is the podcast convention." min="-40" max="0" step="1" value="-23"></span><input type="range" id="lufs-target" min="-40" max="0" step="1" value="-23" style="display:none"><span class="grp vmbay"><span class="ledlbl">&#916;</span><span class="led counter-led" id="lufs-delta-led" title="How far the integrated reading is from the target, in LU. Positive is too loud.">  --.-</span></span><button class="rst" id="rst-lufs-target" title="Reset the loudness target">↺</button></span>
  <span class="pcell axcol swcell" title="Meters — the top-left audio feature strip (amp / bass / mid / treble / cntr / beat). It is a meter, so its switch is in the metering bay rather than on the Console."><span class="punit-top"><span class="plabel">meters</span></span>
    <label class="swline" style="cursor:pointer;" title="Show / hide the top-left audio feature meters (amp / bass / mid / treble / cntr / beat) while Mod is on"><input type="checkbox" class="sw" id="show-meters" checked> Meters</label>
  </span>
</div></div>
<div class="sect gen-osc" id="wf-module"><div class="sect-hdr" title="Wow &amp; Flutter — speed stability, measured off a 3150 Hz test tone. Every other analyzer here asks about amplitude; this asks whether the TIME AXIS is steady, which is what a turntable, a tape deck or a cassette is judged by. Play the Test module's 3150 Hz tone through the deck, capture the result, and read it here: SPEED is the mean frequency error as a percentage (a constant error is a pitch shift, a different fault from a wobble), WOW the slow 0.5–6 Hz modulation that once-per-revolution faults produce, FLUTTER the fast 6–100 Hz kind from capstans and idlers, and W&amp;F the DIN-weighted quasi-peak that a specification quotes — the deviation through a filter peaked at 4 Hz, where the ear is most sensitive to pitch movement. 3150 Hz is the figure every test record carries (DIN 45507, IEC 60386). With no deck in the loop at all it reads 0.000% on the generator itself, which is the check that the instrument is not inventing the number.">Wow &amp; Flutter</div>
<div class="row vmrow meterrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">speed</span><span class="led counter-led" id="wf-speed-led" title="Mean frequency error as a percentage of nominal. Positive is running fast. A constant error is a pitch shift, which is a different fault from a wobble.">  --.---</span></span><span class="grp vmbay"><span class="ledlbl">carr</span><span class="led counter-led" id="wf-carrier-led" title="The carrier as measured, in Hz. Found from the signal rather than assumed, because a deck running fast puts the tone somewhere else.">-----.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">wow</span><span class="led counter-led" id="wf-wow-led" title="Slow modulation, 0.5–6 Hz, RMS as a percentage. An off-center spindle hole is wow at exactly the platter's rate.">  --.---</span></span><span class="grp vmbay"><span class="ledlbl">flut</span><span class="led counter-led" id="wf-flutter-led" title="Fast modulation, 6–100 Hz, RMS as a percentage — capstan and idler faults, which the ear hears as roughness rather than as pitch movement.">  --.---</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">w&amp;f</span><span class="led counter-led" id="wf-weighted-led" title="The DIN-weighted quasi-peak figure, which is the one a specification quotes: the deviation through a filter peaked at 4 Hz, read on meter ballistics that rise quickly and fall slowly. A two-pole approximation of the published curve rather than the tabulated response.">  --.---</span></span><span class="grp vmbay"><span class="led counter-led" id="wf-nom-lbl" title="The nominal frequency the speed error is measured against">3150.0</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">nom</span><input type="number" class="numin gen-led" id="wf-nom-led" title="Nominal carrier frequency in Hz — what the tone is supposed to be. 3150 is what the test records carry. Set it to 0 to measure wow and flutter without a speed figure, which is the honest reading when the tone's true frequency is not known." min="0" max="20000" step="10" value="3150"></span><input type="range" id="wf-nom" min="0" max="20000" step="10" value="3150" style="display:none"><span class="grp vmbay"><span id="wf-nstack"></span></span><button class="rst" id="rst-wf-nom" title="Reset the nominal frequency">↺</button></span>
</div></div>
<div class="sect gen-osc" id="timing-module"><div class="sect-hdr" title="Timing — the rack metering itself. FRAME is the average interval in milliseconds and FPS its reciprocal, taken over the mean rather than the median so one hesitation a second cannot hide behind fifty-nine good frames. MIN and MAX are the best and worst frame in the window. LATE is the share of frames that missed the display's own interval, which is measured rather than assumed: nothing renders faster than vsync, so the shortest frame in a window IS the interval, and the reading means the same on a 60 Hz panel and a 144 Hz one. The four figures below it are where the frame went — MODEL the passes that draw the instrument, METERS everything the rack does to its own audio, SCOPE the tube, and REST the browser's own style, layout, raster and compositing, which is usually the largest share of the four. A rack that draws in 4 ms and spends 9 ms measuring its own audio is not slow, it is mis-apportioned, and no single frame rate says so.">Timing</div>
<div class="row vmrow meterrow">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">fps</span><span class="led counter-led" id="tm-fps-led" title="Frames a second sustained over the last window — the reciprocal of the MEAN frame, so a hitch counts against it">  --.-</span></span><span class="grp vmbay"><span class="ledlbl">frame</span><span class="led counter-led" id="tm-frame-led" title="Average frame interval in milliseconds. 16.7 is a 60 Hz frame; 33.3 means every other one is being missed.">  --.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">min</span><span class="led counter-led" id="tm-min-led" title="Shortest frame in the window, in ms — which is the display's own interval, since nothing renders faster than vsync">  --.-</span></span><span class="grp vmbay"><span class="ledlbl">max</span><span class="led counter-led" id="tm-max-led" title="Longest frame in the window, in ms — the worst single hesitation">  --.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">late</span><span class="led counter-led" id="tm-late-led" title="Percentage of frames that missed the display's interval by half again or more. This is the number that corresponds to visible hesitation; the frame rate alone does not.">  --.-</span></span><span class="grp vmbay"><span class="ledlbl">rest</span><span class="led counter-led" id="tm-rest-led" title="Milliseconds a frame spent outside the rack's own code: style, layout, raster, compositing and the wasm boundary. Usually the largest of the four, and the one no knob on this panel moves.">  --.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">model</span><span class="led counter-led" id="tm-model-led" title="Milliseconds a frame spent ISSUING the model's draw calls. A WebGL call queues a command and returns without waiting for pixels, so this reads far below what the model really costs — the GPU and compositor share of it is in REST. Useful for what it does catch: geometry rebuilt every frame instead of cached.">  --.-</span></span><span class="grp vmbay"><span class="ledlbl">meters</span><span class="led counter-led" id="tm-meters-led" title="Milliseconds a frame the rack spent on its own audio: the tap, the loudness, distortion, wow-and-flutter and counter analyzers, and the sequencer clocks. Unlike MODEL and SCOPE this is arithmetic on this thread and the number IS the cost — it is the one of the four to trust absolutely, and the one the analyzers' own controls move.">  --.-</span></span></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">scope</span><span class="led counter-led" id="tm-scope-led" title="Milliseconds a frame spent building the scope's sweep and handing it to the canvas. Like MODEL this is the issuing cost, not the rasterizing one — a canvas stroke records a path and returns — so the tube's raster lands in REST.">  --.-</span></span></span>
</div></div>
<div class="sect gen-osc keys-sect" id="keys-module"><div class="sect-hdr" title="Keys — a playable polyphonic keyboard: click or drag the keybed (glissando works), or play the computer keyboard on the labeled keys (Z row = lower octave, Q row = upper). The range ring sets the key count up to the full 88; voice and speaker routing live on the out knob.">Keys</div>
<div class="row keysflex">
<div class="vmrow keysctl">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">range</span><span class="led" id="keys-size-led" title="Keyboard size — how many piano keys are on the bed right now">49</span></span><span class="grp vmbay"><span id="keys-rstack"></span></span><button class="rst" id="rst-keys-range" title="Reset the key range">↺</button><select id="keys-span" title="Keyboard range — key count (outer ring: 13 / 25 / 37 / 49 / 61 / 85, or 88 = the full piano, A0–C8)" style="display:none"><option value="1" title="13 keys — one octave">13</option><option value="2" title="25 keys — two octaves">25</option><option value="3" title="37 keys — three octaves">37</option><option value="4" selected title="49 keys — four octaves">49</option><option value="5" title="61 keys — five octaves">61</option><option value="7" title="85 keys — seven octaves">85</option><option value="88" title="88 keys — the whole piano, A0 to C8; the base-octave knob has nothing left to say">88</option></select><select id="keys-base" title="Starting octave — the C the keybed begins on (inner knob; ignored at 88 keys, which always spans A0–C8)" style="display:none"><option value="1" title="C1 — the keybed starts two octaves below middle C">C1</option><option value="2" selected title="C2 — the keybed starts one octave below middle C">C2</option><option value="3" title="C3 — the keybed starts at middle C">C3</option><option value="4" title="C4 — the keybed starts one octave above middle C">C4</option><option value="5" title="C5 — the keybed starts two octaves above middle C">C5</option></select></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="keys-lvl-led" title="Keys output level (0–100)" min="0" max="100" step="1" value="80"></span><input type="range" id="keys-lvl" min="0" max="100" step="1" value="80" style="display:none"><span class="grp vmbay"><span id="keys-lstack"></span></span><button class="rst" id="rst-keys-lvl" title="Reset the level">↺</button></span>
  <span class="pcell axcol vmcell gen-cell gen-out-cell"><span class="punit-top"><span class="plabel">out</span></span><span class="grp vmbay"><span id="keys-ostack"></span></span><button class="rst" id="rst-keys-out" title="Reset the output routing and waveform">↺</button><select id="keys-out" title="Keys output — speaker channel routing (outer ring: off / L / R / L+R)" style="display:none"><option value="off" title="off — the keyboard is muted">off</option><option value="l" title="L — the keyboard plays the left channel only">L</option><option value="r" title="R — the keyboard plays the right channel only">R</option><option value="both" selected title="L+R — the keyboard plays both channels">L+R</option></select><select id="keys-wave" title="Keys voice — oscillator shape (inner knob: sine / tri / sqr / saw / noise)" style="display:none"><option value="0" title="sine — one pure frequency and no harmonics at all">sine</option><option value="1" title="triangle — odd harmonics falling away steeply: soft and hollow">tri</option><option value="2" title="square — odd harmonics falling away slowly: hollow and reedy">sqr</option><option value="3" title="sawtooth — every harmonic, the brightest of the four">saw</option><option value="4" title="noise — a 15-bit shift register; the freq knob sets its playback rate">noise</option></select></span>
</div>
<div class="keys-bed" id="keys-bed" data-no-drag title="Piano keybed — click or hold-and-slide to play; the letter-labeled keys map to your computer keyboard"></div>
</div></div>
<div class="sect gen-osc keys-sect" id="tm-module"><div class="sect-hdr" title="Matrix — a pentatonic tonematrix step sequencer: paint pads on the grid (click or drag; rows are pitches, columns are sixteenth-note steps) and the playhead loops them as pings at the tempo. The pentatonic rows mean any pattern is consonant.">Matrix</div>
<div class="row keysflex">
<div class="vmrow keysctl">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">tempo</span><input type="number" class="numin gen-led" id="tm-tempo-led" title="Tempo (beats per minute) — the playhead steps four columns per beat" min="40" max="300" step="1" value="120"></span><input type="range" id="tm-tempo" min="40" max="300" step="1" value="120" style="display:none"><span class="grp vmbay"><span id="tm-tstack"></span></span><button class="rst" id="rst-tm-tempo" title="Reset the tempo">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">steps</span></span><span class="grp vmbay"><span id="tm-sstack"></span></span><button class="rst" id="rst-tm-steps" title="Reset the step count and root">↺</button><select id="tm-steps" title="Loop length — how many sixteenth-note columns the playhead sweeps (outer ring: 8 / 16 / 32; painted pads survive a change)" style="display:none"><option value="8" title="8 steps — a half-bar loop of sixteenths">8</option><option value="16" selected title="16 steps — one bar of sixteenths">16</option><option value="32" title="32 steps — two bars of sixteenths">32</option></select><select id="tm-root" title="Root octave — the C the bottom row starts on; rows climb the major pentatonic from there (inner knob)" style="display:none"><option value="1" title="C1 — the bottom row starts two octaves below middle C">C1</option><option value="2" title="C2 — the bottom row starts one octave below middle C">C2</option><option value="3" selected title="C3 — the bottom row starts at middle C">C3</option><option value="4" title="C4 — the bottom row starts one octave above middle C">C4</option></select></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="tm-lvl-led" title="Matrix output level (0–100)" min="0" max="100" step="1" value="80"></span><input type="range" id="tm-lvl" min="0" max="100" step="1" value="80" style="display:none"><span class="grp vmbay"><span id="tm-lstack"></span></span><button class="rst" id="rst-tm-lvl" title="Reset the level">↺</button></span>
  <span class="pcell axcol vmcell gen-cell gen-out-cell"><span class="punit-top"><span class="plabel">out</span></span><span class="grp vmbay"><span id="tm-ostack"></span></span><button class="rst" id="rst-tm-out" title="Reset the output routing and waveform">↺</button><select id="tm-out" title="Matrix output — speaker channel routing (outer ring: off / L / R / L+R)" style="display:none"><option value="off" title="off — the matrix is muted">off</option><option value="l" title="L — the matrix plays the left channel only">L</option><option value="r" title="R — the matrix plays the right channel only">R</option><option value="both" selected title="L+R — the matrix plays both channels">L+R</option></select><select id="tm-wave" title="Matrix voice — ping shape (inner knob: sine / tri / sqr / saw / noise)" style="display:none"><option value="0" selected title="sine — one pure frequency and no harmonics at all">sine</option><option value="1" title="triangle — odd harmonics falling away steeply: soft and hollow">tri</option><option value="2" title="square — odd harmonics falling away slowly: hollow and reedy">sqr</option><option value="3" title="sawtooth — every harmonic, the brightest of the four">saw</option><option value="4" title="noise — a 15-bit shift register; the pad's pitch sets its playback rate">noise</option></select></span>
  <span class="pcell axcol vmcell gen-cell"><label class="grp" style="cursor:pointer;" title="Run — start/stop the playhead loop (painting works either way)"><input type="checkbox" class="sw" id="tm-run" checked> Run</label><span class="grp btn-row demo-btn"><button class="pushbtn" id="tm-clear" title="Clear — wipe every pad off the grid"></button><span class="btn-lbl">Clear</span></span></span>
</div>
<div class="tm-grid" id="tm-grid" data-no-drag title="Tonematrix grid — click or drag to paint pads; rows are pentatonic pitches (top = high), columns are steps"></div>
</div></div>
<div class="sect gen-osc keys-sect" id="rhythm-module"><div class="sect-hdr" title="Rhythm — the drum machine a home organ had built into it. Press a preset tab and the section plays that pattern in a loop at the tempo knob's BPM; the tabs interlock, so one plays at a time. The four voices (bass drum, snare, hi-hat, cymbal) are synthesized from oscillators and filtered noise, not samples. Sound out through the L/R ring; if the page is also listening to the system output, the drums drive the model too.">Rhythm</div>
<div class="row keysflex">
<div class="vmrow keysctl">
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">tempo</span><input type="number" class="numin gen-led" id="rhythm-tempo-led" title="Tempo (beats per minute) — beats, not steps: a waltz at 120 has 120 of its three-beat bar's beats a minute, the same as a march at 120" min="40" max="240" step="1" value="100"></span><input type="range" id="rhythm-tempo" min="40" max="240" step="1" value="100" style="display:none"><span class="grp vmbay"><span id="rhythm-tstack"></span></span><button class="rst" id="rst-rhythm-tempo" title="Reset the tempo">↺</button></span>
  <span class="pcell axcol vmcell gen-cell"><span class="punit-top"><span class="plabel">lvl</span><input type="number" class="numin gen-led" id="rhythm-lvl-led" title="Rhythm output level (0–100)" min="0" max="100" step="1" value="80"></span><input type="range" id="rhythm-lvl" min="0" max="100" step="1" value="80" style="display:none"><span class="grp vmbay"><span id="rhythm-lstack"></span></span><button class="rst" id="rst-rhythm-lvl" title="Reset the level">↺</button></span>
  <span class="pcell axcol vmcell gen-cell gen-out-cell"><span class="punit-top"><span class="plabel">out</span></span><span class="grp vmbay"><span id="rhythm-ostack"></span></span><button class="rst" id="rst-rhythm-out" title="Reset the output routing">↺</button><select id="rhythm-out" title="Rhythm output — speaker channel routing (off / L / R / L+R)" style="display:none"><option value="off" title="off — the drum machine is muted">off</option><option value="l" title="L — the drum machine plays the left channel only">L</option><option value="r" title="R — the drum machine plays the right channel only">R</option><option value="both" selected title="L+R — the drum machine plays both channels">L+R</option></select></span>
  <span class="pcell axcol vmcell gen-cell"><label class="grp" style="cursor:pointer;" title="Run — start/stop the rhythm section. Pressing a preset tab starts it too."><input type="checkbox" class="sw" id="rhythm-run"> Run</label><span class="rhythm-beats" id="rhythm-beats" title="Beat lamps — one per beat of the current pattern's bar, lit on the beat you are hearing; the downbeat is the leftmost"></span></span>
</div>
<div class="rhythm-tabs" id="rhythm-tabs" data-no-drag title="Preset tabs — the beat patterns, one at a time. Pressing one pops the last one out and restarts the bar, the way the interlocking tabs on an organ did."></div>
<select id="rhythm-preset" style="display:none"></select>
</div></div>
</div>
<div id="runtime" style="color:#555;font-size:11px;padding-left:40px;"></div>
`

// ── main ─────────────────────────────────────────────────────────────────────

// ledColorDefs is the ORDERED single source for the LED-readout color options.
// It drives the LED-color knob's detents (in sweep order — amber sits between
// red and green), the hidden <select>, the CSS-var color map, and the dial
// dots. Add a color here and it appears everywhere.
var ledColorDefs = []struct{ name, col, glow, bg, bd, desc string }{
	{"red", "#ff3b30", "#ff2a20", "#170000", "#3a0000", "red — the nixie-adjacent readout: highest contrast on black"},
	{"amber", "#ffb000", "#d08000", "#171000", "#3a2a00", "amber — the classic seven-segment LED, and the easiest on the eye"},
	{"green", "#35e06a", "#1c9a44", "#001709", "#0a3a1a", "green — the VFD and early-terminal readout"},
	{"blue", "#3bb0ff", "#1c7ad0", "#001017", "#0a2a3a", "blue — bright and cold; the newest of the six as a real display"},
	{"cyan", "#2ce0e0", "#159a9a", "#001515", "#0a3838", "cyan — the hue the audio-mod readouts used before the LED color was a knob"},  // the old audio-mod readout hue
	{"violet", "#b06cff", "#8040d0", "#12001f", "#2a0a3a", "violet — no display was ever made in it; it is here because it reads well"}, // new
}

// styleKnobRot staggers the knob-style label ring by this many degrees so its
// labels sit between the LED-color dots on the inner ring; the style knob's
// pointer is offset by the same amount so it still points at its labels.
const styleKnobRot = 30.0

// shellFurniture is what lives in the shell alongside the panel: the resize bar
// that straddles the panel's inner edge, and the dock/float cluster that clips
// onto the end of it.
//
// Outside the panel, because the panel scrolls and anything inside it is
// clipped by that. Inside the shell, because the shell is a positioning box
// that moves with the panel — so CSS can place these against whichever edge
// the dock is on, and no code has to recompute where they went.
//
// The dock cluster used to be declared inside the panel's Console module and
// rendered position:fixed out of it, which is why the module width quantizer
// still has to skip out-of-flow children.
const shellFurniture = `
<div id="dock-resize" title="Drag to resize the control panel"></div>
<span class="grp" id="dock-controls" title="Dock the controls to an edge of the window, or detach as a floating panel"><span class="dock-lbl">DOCK</span><button class="ctrl-btn dockb" id="dock-top" title="Dock the panel to the top edge">↑</button><button class="ctrl-btn dockb" id="dock-bottom" title="Dock the panel to the bottom edge">↓</button><button class="ctrl-btn dockb" id="dock-left" title="Dock the panel as a left sidebar">←</button><button class="ctrl-btn dockb" id="dock-right" title="Dock the panel as a right sidebar">→</button><button class="ctrl-btn dockb" id="dock-float" title="Detach the panel as a floating window">⧉</button><button class="ctrl-btn dockb" id="dock-footer" title="Dock the panel inline into the host page's footer, below its own content">▣</button></span>
`
